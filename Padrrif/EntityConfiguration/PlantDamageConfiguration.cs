namespace Padrrif;

public class PlantDamageConfiguration : BaseEntityConfiguration<PlantDamage>
{
    public override void Configure(EntityTypeBuilder<PlantDamage> builder)
    {
        base.Configure(builder);
    }
}
