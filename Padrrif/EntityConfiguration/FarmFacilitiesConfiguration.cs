namespace Padrrif;

public class FarmFacilitiesConfiguration : BaseEntityConfiguration<FarmFacilities>
{
    public override void Configure(EntityTypeBuilder<FarmFacilities> builder)
    {
        base.Configure(builder);
    }
}
