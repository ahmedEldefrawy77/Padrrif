namespace Padrrif;

public class FisheryDamageConfiguration : BaseEntityConfiguration<FisheryDamage>
{
    public override void Configure(EntityTypeBuilder<FisheryDamage> builder)
    {
        base.Configure(builder);
    }
}
