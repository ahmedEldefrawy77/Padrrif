namespace Padrrif;

public class AnimalDamageConfiguration : BaseEntityConfiguration<AnimalDamage>
{
    public override void Configure(EntityTypeBuilder<AnimalDamage> builder)
    {
        base.Configure(builder);
    }
}
